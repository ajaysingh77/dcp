{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "compile",
            "command": "make",
            "args": ["compile"],
            "options": {
                "env": {
                    "BUILD_ARGS": "-gcflags=all=\"-N -l\""
                }
            }
        },

        {
            "type": "shell",
            "label": "build",
            "command": "make",
            "args": ["build"],
            "options": {
                "env": {
                    "BUILD_ARGS": "-gcflags=all=\"-N -l\""
                }
            }
        }
    ]
}